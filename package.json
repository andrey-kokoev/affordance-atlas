{
  "name": "affordance-atlas-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@latest",
  "description": "Closed-loop spatiotemporal affordance availability resolver.",
  "scripts": {
    "dev": "pnpm --filter @affordance-atlas/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
